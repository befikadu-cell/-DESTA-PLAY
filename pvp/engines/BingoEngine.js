import { shuffle, seedFrom, winnersByScore, validateCommonAction } from './_BaseEngine.js';
export const id='bingo', name='Bingo PVP', maxPlayers=8, minPlayers=2;
export function createState(room){ return { numbers:shuffle(Array.from({length:75},(_,i)=>i+1),room.roundId), drawn:[], startedAt:null, claimWindow:false }; }
export function validateAction(action,state){ const a=validateCommonAction(action); if(a.type==='card'){ if(!Array.isArray(a.numbers)||a.numbers.length!==24||new Set(a.numbers).size!==24||a.numbers.some(n=>!Number.isInteger(n)||n<1||n>75)) throw new Error('Bingo card must contain 24 unique numbers from 1 to 75'); return {type:'card',numbers:a.numbers}; } if(a.type==='claim'){ return {type:'claim'}; } if(a.type==='ready') return {type:'ready'}; throw new Error('Unsupported Bingo action'); }
export function resolve(state,players){ const drawn=state.numbers.slice(0,45); const scores=players.map(p=>{const card=Array.isArray(p.actionData?.numbers)?p.actionData.numbers:[]; const hits=card.filter(n=>drawn.includes(n)).length; const claim=p.actionData?.type==='claim'; const valid=hits>=12; return {playerId:String(p.playerId),hits,validClaim:claim&&valid,score:claim&&valid?hits:0};}); return {drawnNumbers:drawn,winners:winnersByScore(scores.filter(x=>x.validClaim)),scores,pattern:'verified 5x5 card / 12+ hits'}; }
export default {id,name,maxPlayers,minPlayers,createState,validateAction,resolve};
